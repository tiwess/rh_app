import { lightColors } from './colors';
import { global } from './global';

export const theme = {
    global,
    colors: lightColors.colors,
    borderRadius: '3px',
    fonts: {
        default: '16px',
        small: '0.875rem',
        smaller: '0.75rem',
    },
};
